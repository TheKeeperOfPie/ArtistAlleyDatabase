package io.ktor.events;

public class EventsKt {

    public static void raiseCatching$default(
            io.ktor.events.Events events,
            io.ktor.events.EventDefinition<?> eventDefinition,
            Object one,
            org.slf4j.Logger logger,
            int integer,
            Object two
    ) {
        // Missing method on Android server, stubbed manually
    }
}
