getSeries:
SELECT rowid AS rowid, * FROM seriesEntry;

insertSeries:
INSERT OR REPLACE INTO seriesEntry
VALUES ?;

deleteSeries:
DELETE FROM seriesEntry
WHERE :id = seriesEntry.id;

getSeriesById:
SELECT rowid AS rowid, * FROM seriesEntry
WHERE :id = seriesEntry.id;
