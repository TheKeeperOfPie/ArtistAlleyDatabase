getArtistId:
SELECT artistId FROM artistFormPublicKey
WHERE publicKey = :publicKey;

insertPublicKey:
INSERT OR REPLACE INTO artistFormPublicKey
VALUES ?;

hasPublicKey:
SELECT EXISTS(SELECT 1 FROM artistFormPublicKey WHERE artistId = :artistId);
